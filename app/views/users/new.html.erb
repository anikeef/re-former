<h1>Create a user</h1>
<% if @user.errors.any? %>
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li>
        <%= message %>
      </li>
    <% end %>
  </ul>
<% end %>

<!-- <form action="/users" method="post" accept-charset="UTF-8">
  <input name="user[username]" type="text" placeholder="User name">
  <input name="user[email]" type="text" placeholder="Email">
  <input name="user[password]" type="password" placeholder="Password">
  <input name="commit" type="submit" value="Submit">
  <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">
</form> -->

<!-- <%= form_tag("/users", method: "post") do %>
  <%= text_field_tag "user[username]", nil, placeholder: "User name" %>
  <%= text_field_tag "user[email]", nil, placeholder: "Email" %>
  <%= password_field_tag "user[password]", nil, placeholder: "Password" %>
  <%= submit_tag "Submit" %>
<% end %> -->

<%= form_for @user do |f| %>
  <%= f.text_field :username, placeholder: "User name" %>
  <%= f.text_field :email, placeholder: "User name" %>
  <%= f.text_field :password, placeholder: "Password" %>
  <%= f.submit "Submit" %>
<% end %>
